import SwapPage from "@/components/dashboard/SwapPage";

export const metadata = {
  title: "Request Shift Swap | Humanova",
  description: "Swap shifts with colleagues and manage roster requests.",
};

export default function SwapRoute() {
  return <SwapPage />;
}
